#/etc/fstab y /etc/mtab: descritos y comparados el actividad1.9
#
#/proc/mounts: proprociona una lista de todos los montajes en usopor el sistema. Se diferencia de
#mtab en que mount esta mas actualizada y es usado por el kernel
#
#/proc/filesystems: contiene una lista de los tipos del sistema de archivos soportados actualmente por el kernel. Tambien es usado por mount

